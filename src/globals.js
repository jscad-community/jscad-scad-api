/**
 * The $fn, $fs and $fa special variables control the number of facets used to generate an arc.
 */
let $fn = 0 // is number of fragments per rotation
let $fs = 2 // is the minimum size of a fragment
let $fa = 12 // is the minimum angle for a fragment

module.exports = { $fn, $fs, $fa }
